# `func_80017A18` findings — consolidated 2026-07-24 (evening)

Target: 279 instructions, stack frame `-0x120` (-288).

Full resume instructions: `HANDOFF_func_80017A18.md`. This file is the
concise summary.

## Verified best

```text
source=src/objects.c (behind #ifdef NON_EQUIVALENT)
score=355  98.73%  max=27900
stack=40  reg=255  reorder=60  insert=0  delete=0
target_insns=279  candidate_insns=279
```

Up from 1472 / 94.72% at the start of this session. Mirrored to
`https://github.com/akratch/dkr-func80017A18-wip` (function alone + full TU).

The source is ordinary C. The 1472 lineage's fakematch and UB scaffolding
(`var_f18 = var_f18`, `if (sum2) {}`, `& 0xFFFFFFFFFFFFFFFF`, comma-operator
probes) is all gone and the clean source is 1072 points ahead of it.

## What is exact now

- instruction count, stack frame, and **instruction mix** (every opcode count);
- **instruction order** — `insert=0`, `delete=0`, one reordering remains;
- all 33 declared-local stack homes;
- long-lived GPR map and all compiler temp slots;
- everything from offset `0x01c0` to the epilogue except register names.

## The four levers (1472 → 400)

| change | effect |
|---|---|
| guarded facet `do/while` with origin saves inside the guard | correct control topology (inherited from the 1790 branch) |
| `sum1 = (A * x2 + D) + (B * y2 + C * z2);` | `insert`/`delete` → 0 |
| `var_a2 = TRUE` moved after the `t` branch | `li a2,1` instead of `move a2,a3` |
| hit point computed z, y, x | −342 |

`var_a2` must be `u32`; `s32` costs ~180.

## What is left

One extra live FP value in the facet-loop head. Our `(A*x2 + D) + (B*y2 +
C*z2)` grouping keeps two partials alive across the final add, so IDO spills
into `f2` for `B*y1` where the target reuses `f14`, and the low FP temp pool
(`f4/f6/f10/f12/f14/f16`) rotates by one. That rotation is essentially the
whole remaining penalty.

The target's chain is flat — `((A*x2 + B*y2) + C*z2) + D` — which needs one
fewer live value. But every flat spelling tried interleaves the three origin
loads with the plane loads instead of hoisting them above the facet-index
computation, gains a `nop` (280 instructions), and scores ~3700. Closing the
gap means finding a source shape that is flat **and** keeps those three loads
at the top of the block.

## Exhausted neighbourhoods (exact scorer throughout)

14,400 term-order × parse-tree combinations for both dot products (400 is the
unique minimum); 5,040 prolog orders; 7,200 prolog staging/order/mul-order
combos; all hit-point orders/forms × `var_a2` positions; plane and edge-plane
load orders; edge-sum trees and comparison spellings; 600 statement-split and
compound-assignment accumulation forms; 309 alternate-accumulator forms;
330 declaration relocations and swaps (declaration order is **inert** for
register allocation here); interleaved flat chains; carrier-variable chains;
control-flow spellings (loop forms, guard forms, `counter`, `j` update, store
order, save/restore/reset orders, `arg2[0]++` spellings); coordinate descent
over 30 axes, re-converged after every axis addition; randomised 1–4 axis
annealing; decomp-permuter with the exact scorer, ~100k iterations across
three bases.

## Measurement

```sh
.venv/bin/python3 objects_research/tools/realscore.py <candidate.c>
.venv/bin/python3 objects_research/tools/realdiff.py  <candidate.c>
```

These drive the repository's own `tools/asm-differ/diff.py` with
`objdump -d -rz -j .text` and reproduce decomp.me exactly. The older
`dscore.py`, edit distance, and word-count metrics are historical diagnostics
only and misrank candidates.
