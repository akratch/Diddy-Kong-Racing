# `func_80017A18` findings — CLOSED 2026-07-24

**Matched.** Score 0 / 100.00%, 279 instructions, frame `-0x120`, all penalty
components zero. In `src/objects.c` compiled from C, `GLOBAL_ASM` removed,
ROM Verify OK.

Superseded by `WRITEUP_func_80017A18.md`, which is the canonical account.
Chronology is in `EXPERIMENT_LOG_2026-07-24.md`.

Two results worth reusing elsewhere:

1. A store/reload pair around a loop may be a compiler spill rather than a
   source-level copy. Check whether the register is reused inside the loop
   before modelling it as a variable.
2. A redundant assignment through an already-dead local emits no instruction
   but rotates IDO's register-allocator phase. Worth ~300 points twice on the
   way to this match.

And one about method: **exact score is not distance to a match.** The move
that finished this function raised the score from 296 to 441. The structural
components (`insert`/`delete`/`reorder`) are the signal worth steering by;
register penalties are a phase problem that resolves once the structure is
right.
