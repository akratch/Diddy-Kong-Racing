===FROM===
            if (trackY < 0 || maxTrackY < trackY || trackX < 0 || trackX >= 6) {
                cur->visible = 0;
            } else {
                cur->visible = 1;
                levelName = level_name(level_world_id(trackY + 1));
                temp = (temp = gTrackSelectIDs[trackY][trackX]);
                selectedTrack = gTrackSelectIDs[trackY][trackX];
                cur->hubName = levelName;
                if (selectedTrack != -1) {
===TO===
                if (selectedTrack != -1) {
===CODE===
            if (trackY < 0 || maxTrackY < trackY || trackX < 0 || trackX >= 6) {
                /*
                 * Express the same element through its offset in the backing
                 * array.  Algebraically this is exactly cur->visible; the
                 * question is whether uopt cancels the round trip while
                 * retaining the direct-array provenance of the store.
                 */
                gTrackSelectRenderDetails[cur - gTrackSelectRenderDetails].visible = 0;
            } else {
                gTrackSelectRenderDetails[cur - gTrackSelectRenderDetails].visible = 1;
                levelName = level_name(level_world_id(trackY + 1));
                cur->hubName = levelName;
                if (gTrackSelectIDs[trackY][trackX] != -1) {
